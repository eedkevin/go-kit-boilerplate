apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: dev
  name: order
  labels:
    project: go-kit-boilerplate
    app: order
spec:
  selector:
    matchLabels:
      namespace: dev
      project: go-kit-boilerplate
      app: order
  replicas: 1
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        namespace: dev
        project: go-kit-boilerplate
        app: order
    spec:
      restartPolicy: Always
      containers:
        - name: order
          image: eedkevin/go-kit-boilerplate:order
          ports:
            - name: http
              containerPort: 5050
            - name: grpc
              containerPort: 5040
            - name: metrics
              containerPort: 9100
          resources:
            limits:
              cpu: 200m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          env:
            - name: APP_NAME
              value: order
            - name: ENV
              value: dev
          volumeMounts:
            - mountPath: /configs/config.yaml
              name: config-volume
              subPath: config.yaml
          imagePullPolicy: Always
      volumes:
        - name: config-volume
          configMap:
            name: order-cfg
